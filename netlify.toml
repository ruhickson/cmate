# Build the web app (Vite) in the web/ folder
[build]
  base = "web"
  command = "npm run build"
  publish = "dist"

# SPA: serve index.html for all routes (optional; only needed if you add client-side routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
